//querySelectorAll
const card = document.querySelectorAll('.card');
console.log(card);

const formularios = document.querySelectorAll('#formulario');
console.log(formularios);

//no existe
const noExiste = document.querySelectorAll('asd');
console.log(noExiste);//nodeList vacio