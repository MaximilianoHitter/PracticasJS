let elemento;

elemento = document;//toooooodo
elemento = document.all;//selecciona las tags
elemento = document.head;//selecciona solo el head con lo que contiene
elemento = document.body;//selecciona solo el body con lo que contiene
elemento = document.domain;//seleeciona el dominio de la pag
elemento = document.forms;//selecciona todos los formularios de la pag
elemento = document.forms[0];//selecciona el primer formularios
elemento = document.forms[0].id;//selecciona el id
elemento = document.forms[0].method;//selecciona el metodo, post o get
elemento = document.forms[0].classList;//clases el form
elemento = document.forms[0].action;//url pero no se que es
elemento = document.links;//Para seleccionar todos los a tag
elemento = document.images;//da cuantas imagenes estas utilizando
elemento = document.scripts;//retorna coleccion con todos los scripts








console.log(elemento);